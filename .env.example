# Cortex environment variables
# Copy to .env.local and fill in. .env.local is gitignored.

# Supabase — leave both blank to run in IndexedDB-only mode (no cloud).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
